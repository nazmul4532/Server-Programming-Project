<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Seller Dashboard</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        h1 {
            color: #333;
        }

        .dashboard-buttons {
            display: flex;
            gap: 20px;
            margin-top: 20px;
        }

        .add-product-button {
            padding: 10px;
            background-color: #4CAF50;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .add-product-button:hover {
            background-color: #45a049;
        }

        .view-products-button {
            padding: 10px;
            background-color: #FFC107;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .view-products-button:hover {
            background-color: #e0a800;
        }

        .view-orders-button {
            padding: 10px;
            background-color: #FF5722;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .view-orders-button:hover {
            background-color: #e64a19;
        }

        .view-profile-button {
            padding: 10px;
            background-color: #3F51B5;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .view-profile-button:hover {
            background-color: #3640a1;
        }

        .logout {
            position: absolute;
            top: 10px;
            right: 10px;
            padding: 10px;
            background-color: #9370DB;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .logout:hover {
            background-color: #8a63c3;
        }
    </style>
</head>

<body>
    <%- include('../../utils/sellerNavBar.ejs') %>

    <h1>Seller Dashboard</h1>
    <div class="dashboard-buttons">
        <button class="add-product-button" onclick="window.location.href='/seller/addProduct'">Add Product</button>
        <button class="view-products-button" onclick="window.location.href='/seller/productsPage'">View
            Products</button>
        <button class="view-orders-button" onclick="window.location.href='/seller/orders'">View Orders</button>
        <button class="view-profile-button" onclick="window.location.href='/seller/profile'">View Profile</button>
    </div>

</body>

</html>
