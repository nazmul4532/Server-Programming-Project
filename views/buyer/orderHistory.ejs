<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order History</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
        }

        h1 {
            text-align: center;
            color: #333;
            margin-bottom: 20px;
        }

        .order-container {
            background-color: #fff;
            padding: 8px; 
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            margin-top: 10px;
            max-width: 300px;
            width: 100%;
            box-sizing: border-box;
            text-align: center;
        }

        .order-details {
            margin-bottom: 8px;
        }

        .view-order-btn {
            background-color: #9370DB;
            color: #fff;
            padding: 6px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            text-decoration: none;
            display: inline-block;
        }

        .view-order-btn:hover {
            background-color: #8a63c3;
        }

        .logout {
            position: absolute;
            top: 10px;
            right: 10px;
            padding: 10px;
            background-color: #9370DB;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .logout:hover {
            background-color: #8a63c3;
        }
    </style>
</head>

<body>
    <%- include('../../utils/buyerNavBar.ejs') %>
    <h1>Order History</h1>

    <% orders.forEach(order => { %>
        <div class="order-container">
            <p class="order-details">Product: <%= order.product.name %></p>
            <p class="order-details">Quantity: <%= order.quantity %></p>
            <p class="order-details">Status: <%= order.status %></p>

            <a class="view-order-btn" href="/buyer/orderDetails/<%= order._id %>">View Order</a>
        </div>
    <% }); %>


</body>

</html>
