<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Buyer Dashboard</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        h1 {
            color: #333;
        }

        .dashboard-buttons {
            display: flex;
            gap: 20px;
            margin-top: 20px;
        }

        .view-products-button {
            padding: 10px;
            background-color: #FFC107;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .view-products-button:hover {
            background-color: #e0a800;
        }

        .view-order-history-button {
            padding: 10px;
            background-color: #4CAF50;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .view-order-history-button:hover {
            background-color: #45a049;
        }

        .view-profile-button {
            padding: 10px;
            background-color: #3F51B5;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .view-profile-button:hover {
            background-color: #3640a1;
        }

        .logout {
            position: absolute;
            top: 10px;
            right: 10px;
            padding: 10px;
            background-color: #9370DB;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .logout:hover {
            background-color: #8a63c3;
        }
    </style>
</head>

<body>
    <%- include('../../utils/buyerNavBar.ejs') %>
    <h1>Buyer Dashboard</h1>
    <div class="dashboard-buttons">
        <button class="view-products-button" onclick="window.location.href='/buyer/productsPage'">View Products</button>
        <button class="view-order-history-button" onclick="window.location.href='/buyer/orderHistory'">Order
            History</button>
        <button class="view-profile-button" onclick="window.location.href='/buyer/profile'">View Profile</button>
    </div>

</body>

</html>
